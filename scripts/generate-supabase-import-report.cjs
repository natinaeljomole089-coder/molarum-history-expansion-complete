const crypto = require("crypto");
const fs = require("fs");
const path = require("path");

const projectRoot = path.resolve(__dirname, "..");
const summary = JSON.parse(fs.readFileSync(path.join(projectRoot, "supabase/imports/molarum-units/import-summary.json"), "utf8"));
const sourcePath = path.join(projectRoot, "assets/question-banks/grade10-source-grounded-bank.json");
const sourceHash = crypto.createHash("sha256").update(fs.readFileSync(sourcePath)).digest("hex");
const unitRows = summary.units.map((unit) => `| ${unit.subject} | ${unit.unit} | ${unit.question_count} | ${unit.choice_count} |`).join("\n");
const content = `# Supabase Question Import Report\n\n## Result\n\nThe Molarum Grade 10 catalog was successfully imported into Supabase project \`jxdfukggxxlemsoumtal\`. The import created **49 quizzes**, **1,960 questions**, and **4,312 choices**. Every unit contains 40 questions.\n\n## Actual source\n\n> The question catalog was **not stored as HTML**. Its canonical source is the bundled JSON file \`assets/question-banks/grade10-source-grounded-bank.json\`, catalog version \`${summary.source.source_catalog_version}\`.\n\n| Source property | Value |\n|---|---|\n| Source format | Canonical Molarum JSON |\n| SHA-256 | \`${sourceHash}\` |\n| Extracted import dataset | \`content/imports/molarum-supabase-question-import.json\` |\n| Questions | ${summary.source.total_questions} |\n| Units | ${summary.source.total_units} |\n\n## Field mapping\n\n| Molarum JSON | Supabase destination |\n|---|---|\n| Question ID prefix and \`unitId\` | \`quizzes.subject\`, \`quizzes.unit\` |\n| \`unitTitle\` | \`quizzes.title\` |\n| \`type\` | \`questions.question_type\` |\n| \`question\` | \`questions.question_text\` |\n| \`answer\` | \`questions.correct_answer\` |\n| \`explanation\` | \`questions.explanation\` |\n| \`options\` | \`choices.choice_text\`, \`choices.is_correct\` |\n\nThe source types \`short_answer\` and \`numerical\` were normalized to the destination's supported \`fill_blank\` value. Original multiple-choice and true/false options were inserted as choices; each has exactly one correct choice.\n\n## Security and execution\n\nThe import used the server-side Supabase management connection. No service-role or secret key was added to the Android source. Import statements use deterministic identifiers and idempotent upserts, so re-running the generated unit files will update the intended records rather than duplicate them.\n\n## Verified counts by unit\n\n| Subject | Unit | Questions | Choices |\n|---|---|---:|---:|\n${unitRows}\n\n## Integrity checks\n\n| Check | Result |\n|---|---:|\n| Imported quizzes | 49 |\n| Imported questions | 1,960 |\n| Imported choices | 4,312 |\n| Orphan imported questions | 0 |\n| Orphan choices | 0 |\n| Choice questions lacking exactly one correct choice | 0 |\n| Fill-blank questions with choices | 0 |\n`;

fs.writeFileSync(path.join(projectRoot, "docs/SUPABASE_QUESTION_IMPORT_REPORT.md"), content);
console.log("Wrote docs/SUPABASE_QUESTION_IMPORT_REPORT.md.");
